guard :livereload do
  watch(%r{^templates/.+\.(css|s[ac]ss|js|coffee|html|erb)})
end

guard :pow do
  watch('Gemfile')
  watch('config.ru')
  watch(%r{^templates/.+\.(css|s[ac]ss|js|coffee|html|erb)})
end
